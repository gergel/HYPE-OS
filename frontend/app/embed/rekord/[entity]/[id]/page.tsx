/** A(z) /rekord/[entity]/[id] generikus adatlap keret nélküli változata,
 * felugró ablakhoz (lásd components/RecordDetailModal.tsx). Szándékosan
 * UGYANAZT a komponenst exportálja újra, mint a rendes oldal. */
export { default } from "@/app/(app)/rekord/[entity]/[id]/page";
